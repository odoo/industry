<?xml version='1.0' encoding='UTF-8'?>
<odoo noupdate="1">
    <!-- Le générer automatiquement depuis la SO-->
  <!-- <record id="account_move_1" model="account.move">
    <field name="invoice_date" eval="datetime.today().date() + relativedelta(days=3)"/>
    <field name="company_id" ref="base.main_company"/>
    <field name="move_type">out_invoice</field>
    <field name="date" eval="datetime.today().date() + relativedelta(days=3)"/>
    <field name="invoice_date_due" eval="datetime.today().date() + relativedelta(days=-1)"/>
    <field name="invoice_currency_rate">1.0</field>
    <field name="narration"><![CDATA[<div data-oe-version="2.0">A monthly invoice will be generated using actual, collected data.</div>]]></field>
    <field name="invoice_user_id" ref="base.user_admin"/>
    <field name="invoice_origin">S00001</field>
  </record> -->
</odoo>
