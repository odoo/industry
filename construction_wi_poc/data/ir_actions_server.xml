<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <record id="action_create_delivery_location_on_sales_order_confirm" model="ir.actions.server">
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="name">Construction Developer: Create Delivery Location</field>
        <field name="code">
for so in records.filtered(lambda so: (so.state != 'draft' or env.context.get('force_loc', False)) and not so.partner_shipping_id.x_ws_location_id):
    wh = env['ir.model.data']._xmlid_to_res_id('construction_wi_poc.stock_location_worksites')
    loc = env['stock.location'].create({'name': f"{so.partner_shipping_id.name}", 'usage': 'internal', 'location_id': wh, 'replenish_location': True, 'company_id': env.company.id})
    so.partner_shipping_id['x_ws_location_id'] = loc
</field>
    </record>
    <record id="action_apply_locations_on_mo" model="ir.actions.server">
        <field name="model_id" ref="mrp.model_mrp_production"/>
        <field name="state">code</field>
        <field name="name">Construction Developer: apply locations on Manufactoring Order</field>
        <field name="code">
for mo in records:
    mo['location_src_id'] = (so := mo.sale_line_id.order_id).partner_shipping_id.x_ws_location_id
    if mo.sale_line_id.x_bom_id:
        new_bom = mo.sale_line_id.x_bom_id.copy({'code': f"{so.name} - {so.partner_id.name}"})
        mo['bom_id'] = new_bom
</field>
    </record>
</odoo>
