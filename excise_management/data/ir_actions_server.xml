<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <record id="add_excise_server_action" model="ir.actions.server" forcecreate="1">
        <field name="code"><![CDATA[if (excise_tax := env["account.tax"].search([("x_is_excise", "=", "True")], limit=1)):
    for product in records:
        record['taxes_id'] = [Command.link(excise_tax.id)]
]]></field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="name">Add excise tax</field>
    </record>
    <record id="remove_excise_server_action" model="ir.actions.server" forcecreate="1">
        <field name="code"><![CDATA[if (excise_tax := env["account.tax"].search([("x_is_excise", "=", "True")], limit=1)):
    for product in records:
        record['taxes_id'] = [Command.unlink(excise_tax.id)]
]]></field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="state">code</field>
        <field name="name">Remove excise tax</field>
    </record>
    <record id="convert_to_base_unit_server_action" model="ir.actions.server">
        <field name="binding_model_id" ref="uom.model_uom_uom"/>
        <field name="code"><![CDATA[def convert_to_base_unit(uom):
    if uom.relative_uom_id: 
        return uom.relative_factor * convert_to_base_unit(uom.relative_uom_id)
    else: 
        return uom.relative_factor
action = convert_to_base_unit(record)]]></field>
        <field name="model_id" ref="uom.model_uom_uom"/>
        <field name="state">code</field>
        <field name="name">Convert to base unit</field>
    </record>
</odoo>
