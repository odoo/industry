<?xml version='1.0' encoding='UTF-8'?>
<odoo>
    <!-- 
        This feature adds automations to modify the cost and vendor lists based on purchase orders. 
    -->
    
    <record model="ir.actions.server" id="action_update_product_cost_and_vendor_list">
        <field name="name">Construction Developer: Update Product Cost and Vendor List</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="state">code</field>
        <field name="code">
partner_id, qty, price = (env.context.get(k) for k in ('partner_id', 'qty', 'price'))
if supplier_info := env['product.supplierinfo'].search([('partner_id', '=', partner_id), ('product_tmpl_id', '=', record.product_tmpl_id.id), ('product_id', 'in', [record.id, False]), '|', ('min_qty', '=', qty), ('price', '=', price)], limit=1):
    if not supplier_info.product_id:    supplier_info['product_id'] = record.id
    if float_compare(price, supplier_info.price, precision_digits=2) == 0:
        supplier_info['min_qty'] = qty
    elif float_compare(qty, supplier_info.min_qty, precision_digits=2) == 0:
        supplier_info['price'] = price
else:
    supplier_info = env['product.supplierinfo'].create({'partner_id': partner_id, 'product_tmpl_id': record.product_tmpl_id.id, 'product_id': record.id, 'min_qty': qty, 'price': price, 'delay': 0})
record['standard_price'] = price
((env['product.supplierinfo'].search([('partner_id', '=', partner_id), ('product_tmpl_id', '=', record.product_tmpl_id.id), ('product_id', '=', record.id), ('min_qty', '>=', qty), ('price', '>=', price)])) - supplier_info).unlink()
</field>
    </record>


    <record model="ir.actions.server" id="action_update_product_cost_and_vendor_list_from_purchase_order">
        <field name="name">Construction Developer: Update Product Cost and Vendor List (from Purchase Order)</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="usage">base_automation</field>
        <field name="state">code</field>
        <field name="code">
for record in records:
    partner_id = record.partner_id.id if not record.partner_id.parent_id else record.partner_id.parent_id.id
    for line in record.order_line:
        server_action = env.ref('construction_developer.action_update_product_cost_and_vendor_list')
        server_action.with_context(active_id=line.product_id.id, active_model="product.product", partner_id=partner_id, qty=line.product_qty, price=line.price_unit).sudo().run()
</field>
    </record>

    <record model="base.automation" id="automation_update_product_cost_and_vendor_list_from_purchase_order">
        <field name="name">Construction Developer: On Purchase Order confirmation</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="trigger">on_state_set</field>
        <field name="filter_domain">[('state', '=', 'purchase')]</field>
        <field name="trigger_field_ids" eval="[(6, 0, [ref('purchase.field_purchase_order__state')])]"/>
        <field name="trg_selection_field_id" ref="purchase.selection__purchase_order__state__purchase"/>
        <field name="action_server_ids" eval="[(6, 0, [ref('action_update_product_cost_and_vendor_list_from_purchase_order')])]"/>
    </record>


    <record model="ir.actions.server" id="action_update_product_cost_and_vendor_list_from_vendor_bill">
        <field name="name">Construction Developer: Update Product Cost and Vendor List (from Vendor Bill)</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="usage">base_automation</field>
        <field name="state">code</field>
        <field name="code">
for record in records:
    partner_id = record.partner_id.id if not record.partner_id.parent_id else record.partner_id.parent_id.id
    server_action = env.ref('construction_developer.action_update_product_cost_and_vendor_list')
    for line in record.invoice_line_ids:
        if not line.product_id: continue
        server_action.with_context(active_id=line.product_id.id, active_model="product.product", partner_id=partner_id, qty=line.quantity, price=line.price_unit).sudo().run()
</field>
    </record>

    <record model="base.automation" id="automation_update_product_cost_and_vendor_list_from_vendor_bill">
        <field name="name">Construction Developer: On Vendor Bill confirmation</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="trigger">on_state_set</field>
        <field name="filter_pre_domain">[("move_type", "=", "in_invoice")]</field>
        <field name="filter_domain">[('state', '=', 'posted')]</field>
        <field name="trigger_field_ids" eval="[(6, 0, [ref('account.field_account_move__state')])]"/>
        <field name="trg_selection_field_id" ref="account.selection__account_move__state__posted"/>
        <field name="action_server_ids" eval="[(6, 0, [ref('action_update_product_cost_and_vendor_list_from_vendor_bill')])]"/>
    </record>
</odoo>
